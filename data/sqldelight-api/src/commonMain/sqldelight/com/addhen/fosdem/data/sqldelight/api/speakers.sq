CREATE TABLE IF NOT EXISTS `speakers` (
  `id` INTEGER NOT NULL,
  `name` TEXT NOT NULL,
  PRIMARY KEY(`id`)
);

-- queries
insert:
INSERT OR IGNORE INTO speakers (
  `id`,
  `name`
) VALUES(
  nullif(:id, 0),
  :name
);

