CREATE TABLE IF NOT EXISTS `speakers` (
  `id` INTEGER PRIMARY KEY,
  `name` TEXT NOT NULL
);

-- queries
insert:
INSERT OR IGNORE INTO speakers (
  `id`,
  `name`
) VALUES(
  nullif(:id, 0),
  :name
);

